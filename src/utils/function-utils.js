/**
 * A function that accepts no parameters and returns nothing
 */
export function noop() {}

/**
 * A function that returns its argument unmodified
 * @param {*} v
 * @returns {*} v
 */
export function identity(v) {
  return v;
}
